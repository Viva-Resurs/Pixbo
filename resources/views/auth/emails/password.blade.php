<?php

//echo "About to send";

// the message
//$msg = "Klicka på följande länk för att återställa ditt lösenord\n\n<a href='http://pixbo-revision.dev/auth/reset/?token=$token'>Återställ Lösenord</a>";

/*
if (isset($message)){
	var_dump($message);
}
else if (isset($reset)){
	echo $reset;
}
else
	echo "No data...";
*/

// use wordwrap() if lines are longer than 70 characters
//$msg = wordwrap($msg,70);

// send email
//mail($email,"TEST",$msg);

//echo "Sent";

//phpinfo();