Features:

    * Flödesschema

    * Schemalägg uppdatering av shadowevents.
        * Loggar?

    * API REST

    * Tags
        * Push tags into an array, display properly.

* Settings
    * User
        * Role -- Does not update the value of the model......

    * Dashboard (Admin)
        * Log
        * Player
            * Tickers
                * fade/print?
            * Vegas
                * Timer bar

    * Forms/Edits:
        Update tags when creating new
        Checkbox for Screengroup-association
        Confirm Delete/Remove Association
        Redirect to show post after edit

    * Users:
        Log edits

    * Screens:
        Smaller thumbs
    
    * Player:
        Carusell

Bugs:
    * When a user changes language the vue-translate doesn't update it's lang files unless the user manually refreshes the site.


Remember:
    * Set PHP memory size and upload file size properly.
    * Set environment variable for phpunit \vendor\bin

Done:
    Validation warn if no tag